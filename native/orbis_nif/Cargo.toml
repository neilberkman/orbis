[package]
name = "orbis_nif"
version = "0.1.0"
authors = []
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
rustler = "0.37.3"
chrono = "0.4"
# Lane pin until the promoted D1 RTK filter kernel is published to crates.io.
astrodynamics = "0.9.0"
astrodynamics-gnss = "0.12.0"
